# ct-samplesize ROADMAP — 图形输出扩展待办

> 2026-08-28 建立。依据：用户提供的 6 大类图形参考清单（非全部实现要求），
> 结合引擎/依赖现状分类。★ = 高频必出，☆ = 特定场景。
> 状态图例：✅ 已实现 / 🟢 待办（条件已具备）/ 🟡 待办（需其他条件）/ ❌ 不做

## 一、单变量曲线（引擎 .curve_solvers 原生支持）

| 图形 | 优先级 | 状态 | 备注 |
|---|---|---|---|
| 效能-样本量曲线 | ★ | ✅ | `run_task.R:.curve_solvers` 已支持 9 方法（ttest×3/ANOVA/比例×2/生存/等效/BE-TOST）；默认全图形集含曲线+分布图+效应轴+热力图（2026-08-28，带 `type` 字段），展示走本地 HTML 报告 |
| 样本量-效应量曲线 | ★ | ✅ | `--effect_seq` 连续效应量 X 轴 + `--power_seq` → y=所需样本量（2026-08-28 新增） |
| 效能-效应量曲线 | ☆ | ✅ | `--effect_seq` + `--n_seq`/`--nobs` → y=效能（2026-08-28 新增） |
| H0/H1 分布重叠图 | ★ | ✅ | `--dist_plot` 已实现（2026-08-28）；标准化效应空间画两条正态密度并着色 α/β 区，base R 直出，支持 ttest*/proportion*/survival |
| 样本量-α 曲线 | ☆ | 🟡 | 求解器加 α 扫描即可；冷门场景，收益低 |

> 注：`--plot_effects`（多系列离散叠加）与 `--effect_seq`（连续轴曲线）互补；
> 前者画 2–3 条档位曲线做敏感性，后者把效应量作为 X 轴反推临界效应量。

## 二、二维联合 / 热力图

| 图形 | 优先级 | 状态 | 备注 |
|---|---|---|---|
| 效能热力图 / 等效能等高线 | ★ | ✅ | `--heatmap` 已实现（2026-08-28）；`--n_seq` × `--effect_seq` 网格算效能，base R `filled.contour` 渲染（**无需 ggplot2**，与原假设一致） |

## 三、生存分析

| 图形 | 优先级 | 状态 | 备注 |
|---|---|---|---|
| 事件数-随访时间图 | ★ | 🟢 | `--power_time_seq` 已实现效能-随访时长曲线（含 events 数组与失访率建模，2026-08-28）；事件数-随访时间视角可由同一函数返回的 `events` 数组直接绘制 |
| 效能-随访时长/失访率图 | ☆ | ✅ | `--power_time_seq` 已实现（2026-08-28，survival 仅）；指数事件累积（uniform accrual + 指数 dropout）闭式算 E(τ) 与效能(τ)，标注达到 target power 时长；需 `--event_rate`+`--accrual_time`（单位一致） |

## 四、稳健性 / 比较 / 序贯 / 流程

| 图形 | 优先级 | 状态 | 备注 |
|---|---|---|---|
| 灵敏度分析图（参数区间带） | ☆ | 🟢 | 一次请求内循环扫描 SD/比例区间，带区间绘制 |
| 组序贯边界图 / 累积效能 | ☆ | 🟢 | rpact 已在 tier1；group_sequential/adaptive 已实现，调 rpact 边界函数出图 |
| 样本量-α 曲线 | ☆ | 🟡 | 求解器加 α 扫描即可；冷门场景，收益低 |
| 参数 vs 非参数效能对比 | ☆ | 🟡 | 非参数效能无闭式解，需 Monte-Carlo 模拟（计算量×K 倍），口径争议大 |
| 检验选择树 / 计算流程图 | ☆ | ❌ | SOP/决策文档类，非统计计算图；应由 LLM/手工生成，R 引擎无意义 |

## 实施顺序建议

1. **分布重叠图** ✅（2026-08-28 已实现 `--dist_plot`）
2. **效能热力图** ✅（2026-08-28 已实现 `--heatmap`，base R `filled.contour`）
3. **事件数-随访时间图 / 效能-随访时长图** ✅（2026-08-28 已实现 `--power_time_seq`，survival 仅）
4. 余下 🟡/❌ 项（样本量-α 曲线 / 组序贯边界 / 非参数对比 / 检验选择树）维持原判定，非当前优先级。

## 依赖备注

- ggplot2：用户确认已在 coze 部署环境；但本批新增图（分布重叠/热力图）统一走 base R（`polygon`/`filled.contour`）+ svglite，**不依赖 ggplot2**，规避「部署完才发现包缺失」风险。
- 新增图全部在 coze 端 R 引擎（run_task.R 新分支 + dispatcher），**重新部署 coze 包后生效**。
- 契约修正（2026-08-28）：survival 契约 `required` 增补 `event_rate`/`accrual_time`，使 `--power_time_seq` 与主线 survival 计算均按用户指定事件率/入组时长取值（此前 R 端 `%||%` 默认 0.05/1 静默覆盖用户输入）。
